apiVersion: apps/v1
kind: Deployment
metadata:
  name: scraang-deployment
  namespace: default
  labels:
    app: scraang
    tier: frontend
    environment: production
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: scraang
      environment: production
  template:
    metadata:
      labels:
        app: scraang
        tier: frontend
        environment: production
    spec:
      restartPolicy: Always    
      containers:
      - name: scraper
        image: nliakm/scraang-scraper:0.1
        imagePullPolicy: Always
        # command:
        #   - /root/scraper.py
        volumeMounts:
          - mountPath: /var/offers
            name: www
            subPath: files
        env:
          - name: HOUR
            value: "23"
          - name: MIN
            value: "59"
          - name: MARKETNAMES
            value: "['REWE','Penny-Markt']"
          - name: LATITUDE
            valueFrom:
              secretKeyRef:
                name: default-secrets
                key: SCRAANG_LAT
          - name: LONGITUDE
            valueFrom:
              secretKeyRef:
                name: default-secrets
                key: SCRAANG_LON
      - name: flask
        image: nliakm/scraang-flask:0.1
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        volumeMounts:
          - mountPath: /var/offers
            name: www
            subPath: files        
        livenessProbe:
          failureThreshold: 5
          httpGet:
            path: /
            port: 5000
            scheme: HTTP
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: 5000
            scheme: HTTP
          periodSeconds: 30        
      volumes:
        - name: www
          persistentVolumeClaim:
            claimName: scraang-www
       